const translations = {
  ru: {
    brandName: "Лочин Уайльд",
    brandRole: "Композитор, саунд‑дизайнер и звукорежиссёр",
    heroKicker: "Музыка, звук и атмосфера",
    heroTitle:
      "Создаю музыку и звук, которые усиливают истории игр, фильмов и брендов",
    heroSubtitle:
      "С 2009 года работаю с аудио, с 2011 — углублённо занимаюсь композицией, саунд‑дизайном и сведением. Помогаю проектам звучать цельно и эмоционально.",
    heroCta: "Услуги и форматы",
    heroSecondary: "Обсудить ваш проект",
    heroCardLabel: "Коротко обо мне",
    heroCardText:
      "Я создаю оригинальную музыку, звуковые миры и чистый, аккуратный микс. Люблю драматургию звука: от первых набросков до финального мастеринга.",
    navAbout: "Обо мне",
    navServices: "Услуги",
    navWorks: "Музыка",
    navPrograms: "Программы",
    navContacts: "Контакты",
    navToggleLabel: "Меню",
    aboutTitle: "Кто я",
    aboutLead:
      "Лочин Уайльд, 29 лет. Композитор, саунд‑дизайнер и звукорежиссёр, в музыке с 2009 года, профессионально — с 2011.",
    aboutBody:
      "Работаю на стыке музыки и звука: пишу оригинальные саундтреки, создаю саунд‑дизайн и занимаюсь сведением/мастерингом. Мне важны драматургия, настроение и то, как звук помогает истории раскрыться.\n\nОткрыт к работе с играми, кино и сериалами, рекламой, онлайн‑курсами, подкастами и личными брендами. Форматы — от точечных задач до полного сопровождения проекта по звуку.",
    servicesTitle: "Услуги",
    servicesLead:
      "Основные направления, в которых я помогаю проектам звучать сильнее.",
    service1Title: "Музыка для игр, фильмов и брендов",
    service1Text:
      "Написание оригинальной музыки под драматургию проекта: от минималистичных тем до плотных оркестровых аранжировок и электроники.",
    service2Title: "Саунд‑дизайн и атмосферы",
    service2Text:
      "Создание звуковых миров: окружение, интерфейсы, эффекты, фолею и акценты, которые собирают картинку в цельную реальность.",
    service3Title: "Сведение и мастеринг",
    service3Text:
      "Чистый баланс, глубина и динамика. Подготовка материала к релизу или интеграции в игру, фильм, рекламу или платформы.",
    worksTitle: "Музыка и релизы",
    worksLead:
      "Послушайте прямо здесь, как я работаю с настроением и пространством.",
    worksCta: "Слушать музыку на всех платформах",
    playerTitle: "Слушать на SoundCloud",
    playerNote:
      "Плеер загрузится по нажатию — до этого страница не обращается к SoundCloud.",
    programsTitle: "Программы",
    programsLead:
      "Инструменты, которые я делаю для себя и для коллег по цеху.",
    sortirTitle: "Музыкальный сортир",
    sortirBadge: "Закрытое тестирование",
    sortirText:
      "Раскладывает диджейскую фонотеку по жанрам, определяет BPM и тональность в системе Camelot, выравнивает громкость. Работает целиком в браузере: ничего не нужно устанавливать, а файлы никуда не загружаются — весь разбор идёт на вашем компьютере.",
    sortirFeature1: "Разбор фонотеки по жанрам",
    sortirFeature2: "BPM и тональность в Camelot",
    sortirFeature3: "Выравнивание громкости",
    sortirFeature4: "Ничего не нужно устанавливать",
    sortirCta: "Открыть программу",
    sortirSource: "Исходный код на GitHub",
    sortirNote:
      "Вход по коду приглашения: программа пока в закрытом тестировании.",
    contactsTitle: "Контакты",
    contactsLead: "Выберите удобный способ связи, чтобы обсудить задачу.",
    contactsInstagram: "Instagram",
    contactsTelegram: "Telegram‑канал",
    footerCopy: `© ${new Date().getFullYear()} Лочин Уайльд`,
  },
  en: {
    brandName: "Lochin Wilde",
    brandRole: "Composer, sound designer & mixing engineer",
    heroKicker: "Music, sound and atmosphere",
    heroTitle:
      "I craft music and sound that amplify stories of games, films and brands",
    heroSubtitle:
      "In music since 2009, working deeply with composition, sound design and mixing since 2011. I help projects sound focused, emotional and cohesive.",
    heroCta: "View services",
    heroSecondary: "Discuss your project",
    heroCardLabel: "A few words about me",
    heroCardText:
      "I create original music, detailed sound worlds and a clean, balanced mix. I care about narrative and how sound supports emotion.",
    navAbout: "About",
    navServices: "Services",
    navWorks: "Music",
    navPrograms: "Programs",
    navContacts: "Contacts",
    navToggleLabel: "Menu",
    aboutTitle: "Who I am",
    aboutLead:
      "Lochin Wilde, 29. Composer, sound designer and mixing engineer — in music since 2009, more in‑depth since 2011.",
    aboutBody:
      "I work where music meets sound: composing original scores, building soundscapes and handling mixing/mastering. I focus on storytelling, mood, and the way audio helps the project speak.\n\nOpen to working with games, films and series, ads, online courses, podcasts and personal brands. Formats range from one‑off tasks to full audio supervision.",
    servicesTitle: "Services",
    servicesLead: "Key ways I can help your project sound stronger.",
    service1Title: "Music for games, film and brands",
    service1Text:
      "Original music tailored to your narrative: from subtle themes to dense orchestral textures and electronic scores.",
    service2Title: "Sound design & ambience",
    service2Text:
      "Creating sound worlds: ambience, UI and gameplay SFX, foley and emotional accents that make visuals feel alive.",
    service3Title: "Mixing & mastering",
    service3Text:
      "Clear balance, depth and dynamics. Preparing material for release or integration into games, film, ads or platforms.",
    worksTitle: "Music and releases",
    worksLead: "Listen right here to how I work with mood, space and dynamics.",
    worksCta: "Listen on all platforms",
    playerTitle: "Listen on SoundCloud",
    playerNote:
      "The player loads when you press it — until then this page does not contact SoundCloud.",
    programsTitle: "Programs",
    programsLead: "Tools I build for myself and for fellow DJs.",
    // Transliterated, not translated. "Сортир" is coarse slang for a toilet,
    // so an English rendering of the joke reads far ruder than the Russian
    // does. The name travels as a name.
    sortirTitle: "Muzykalny Sortir",
    sortirBadge: "Closed beta",
    sortirText:
      "Sorts a DJ library by genre, detects BPM and Camelot key, and evens out loudness. It runs entirely in the browser: nothing to install, and your files are never uploaded — everything is analysed on your own machine.",
    sortirFeature1: "Library sorted by genre",
    sortirFeature2: "BPM and Camelot key",
    sortirFeature3: "Loudness levelling",
    sortirFeature4: "Nothing to install",
    sortirCta: "Open the app",
    sortirSource: "Source on GitHub",
    sortirNote: "Entry is by invite code — the app is still in closed testing.",
    contactsTitle: "Contacts",
    contactsLead: "Choose a convenient way to get in touch.",
    contactsInstagram: "Instagram",
    contactsTelegram: "Telegram channel",
    footerCopy: `© ${new Date().getFullYear()} Lochin Wilde`,
  },
};

function applyLanguage(lang) {
  const dict = translations[lang];
  if (!dict) return;

  document.documentElement.lang = lang;

  const elements = document.querySelectorAll("[data-i18n]");
  elements.forEach((el) => {
    const key = el.getAttribute("data-i18n");
    if (!key || !(key in dict)) return;

    const value = dict[key];
    if (typeof value === "string" && value.includes("\n")) {
      // Blank-line-separated text becomes real paragraphs. textContent is used
      // per paragraph rather than innerHTML on the whole block, so a translation
      // string can never introduce markup.
      el.textContent = "";
      value.split("\n\n").forEach((block) => {
        const p = document.createElement("p");
        p.textContent = block;
        el.appendChild(p);
      });
    } else {
      el.textContent = value;
    }
  });

  // Labels that are read out rather than shown. The menu button has no text of
  // its own -- it is three lines -- so without this it stays English for a
  // Russian screen-reader user.
  document.querySelectorAll("[data-i18n-aria]").forEach((el) => {
    const key = el.getAttribute("data-i18n-aria");
    if (key && key in dict) el.setAttribute("aria-label", dict[key]);
  });

  // Which language is current is now a property of the address, so the switch
  // marks itself in the markup of each page rather than being set from here.
}

document.addEventListener("DOMContentLoaded", () => {
  /*
   * The page states its own language, and that is the only thing consulted.
   *
   * This used to read navigator.language and rewrite the page to match, which
   * had two costs. A visitor on /en/ with a Russian browser was served Russian
   * on an English URL, and every visitor saw the markup for a moment before the
   * script replaced it. Now each address is one language throughout, and
   * switching is a link rather than a rewrite.
   *
   * The dictionaries still run, because they carry what markup cannot: the
   * current year in the footer, and the aria-labels for controls that have no
   * text of their own.
   */
  applyLanguage(document.documentElement.lang === "en" ? "en" : "ru");

  /*
   * Menu for narrow screens.
   *
   * aria-expanded on the button is the single source of truth: the CSS reads it
   * for the open/closed icon, and assistive technology reads it for the state.
   * Keeping the class on the nav in step with it means there is one thing to
   * get right rather than two that can disagree.
   */
  const navToggle = document.getElementById("nav-toggle");
  const mainNav = document.getElementById("main-nav");

  if (navToggle && mainNav) {
    const setMenu = (open) => {
      navToggle.setAttribute("aria-expanded", String(open));
      mainNav.classList.toggle("is-open", open);
    };

    navToggle.addEventListener("click", () => {
      setMenu(navToggle.getAttribute("aria-expanded") !== "true");
    });

    // Following a link scrolls the page behind the open panel, so it has to
    // close itself; otherwise the section the user picked is hidden by it.
    mainNav.addEventListener("click", (event) => {
      if (event.target.closest("a")) setMenu(false);
    });

    document.addEventListener("keydown", (event) => {
      if (event.key !== "Escape") return;
      if (navToggle.getAttribute("aria-expanded") !== "true") return;
      setMenu(false);
      // Focus goes back to what opened the panel, rather than being left on an
      // element that is now hidden.
      navToggle.focus();
    });

    // A panel left open while the layout widens back into a horizontal bar
    // would apply .is-open to a nav that is no longer a panel.
    window.matchMedia("(min-width: 881px)").addEventListener("change", (event) => {
      if (event.matches) setMenu(false);
    });
  }

  /*
   * The SoundCloud embed, loaded on demand.
   *
   * Building the iframe here rather than shipping it in the HTML is what keeps
   * the page free of third-party code until a visitor asks for music. The
   * button is replaced rather than hidden, so nothing is left behind to tab to.
   *
   * auto_play is on because the only way to get here is by pressing play, and
   * a player that then needs a second press reads as broken.
   */
  const playerBox = document.getElementById("player");
  const playerButton = document.getElementById("player-load");

  if (playerBox && playerButton) {
    playerButton.addEventListener("click", () => {
      const frame = document.createElement("iframe");
      frame.src =
        "https://w.soundcloud.com/player/?url=" +
        encodeURIComponent("https://soundcloud.com/lochin") +
        "&color=%232563eb&auto_play=true&show_user=true&hide_related=true";
      frame.title = "SoundCloud";
      frame.allow = "autoplay";
      frame.loading = "lazy";
      // Nothing here needs to reach back into the page, and the embed is
      // third-party code, so it gets the narrowest sandbox that still plays:
      // its own scripts and its own origin, no forms and no top-level
      // navigation of this page.
      frame.setAttribute("sandbox", "allow-scripts allow-same-origin allow-popups");
      playerBox.replaceChildren(frame);
    });
  }

  const revealItems = document.querySelectorAll("[data-reveal]");
  const prefersReducedMotion = window.matchMedia(
    "(prefers-reduced-motion: reduce)"
  ).matches;

  if (!prefersReducedMotion && "IntersectionObserver" in window) {
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add("is-visible");
            observer.unobserve(entry.target);
          }
        });
      },
      {
        threshold: 0.2,
      }
    );

    revealItems.forEach((el, index) => {
      el.style.transitionDelay = `${index * 0.06}s`;
      observer.observe(el);
    });
  } else {
    revealItems.forEach((el) => {
      el.classList.add("is-visible");
    });
  }
});

